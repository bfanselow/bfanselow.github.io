import  React, { FC } from "react";
import { Box } from "@mui/material";

const LoginPage: FC = () => {

    return (
        <Box>
            Hello!
        </Box>
    );
};

export default LoginPage;
